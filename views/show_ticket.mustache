<!DOCTYPE html>
<html lang="en">

{{> head}}

<body id="page-top">
    {{> navigation}}

    <!-- Portfolio Grid-->
    <section class="page-section bg-light" id="buyPass">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="modal-body">
                        <div class="text-center">
                            <h2 class="section-heading text-uppercase" id="titleTicket">{{ticket.titleTicket}}</h2>
                            <br></br> <!-- salto de línea-->
                        </div>
                        <!-- Portfolio item 1-->
                        <img class="img-fluid d-block mx-auto" src="/assets/img/portfolio/2.jpg" alt="..." />
                        <br />
                        <p id="titleInfoEvent" style="opacity:0">{{event.titleEvent}}</p>
                        <div class="text-center">
                            <p class="text-center"></p>
                            <h3 id="priceTicket">Precio: {{ticket.price}}</h3>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Botón volver atrás -->
        <div class="btnAdd">
            <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/1.3.2/jspdf.min.js"></script>
            <button class="btn btn-primary btn-xl text-uppercase" onclick="save()">Comprar</button>
            <a class="btn btn-primary btn-xl text-uppercase" href="/event/{{event.id}}">Volver
                atrás</a>
        </div>
    </section>

    {{> footer}}

    <!-- Bootstrap core JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Core theme JS-->
    <script src="https://cdn.startbootstrap.com/sb-forms-latest.js"></script>
    <script src="js/scriptPDF.js"></script>
    <script src="https://cdn.startbootstrap.com/sb-forms-latest.js"></script>
</body>

</html>